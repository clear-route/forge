// Package config provides configuration management for the Forge framework.
package config

// Config holds the configuration for a Forge agent.
type Config struct {
	// Configuration fields will be added as the framework develops
}
